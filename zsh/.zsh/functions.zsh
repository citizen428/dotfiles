autoload -U zmv

fpath=($fpath ~/.zsh/functions)
autoload -U brew-deps
autoload -U clone
autoload -U csvcat
autoload -U devs
autoload -U emacs-install
autoload -U mcd
autoload -U prof
autoload -U timezsh
